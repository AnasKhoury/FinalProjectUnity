# Android AR Notes

The current phone project documentation is maintained in:

```text
README.md
```

Use that file for the latest Android build steps, Teachable Machine model replacement flow, object labels, audio controls, and debug-panel instructions.

Important: do not run `Museum AR > Initialize Android AR Project` unless you intentionally want to recreate the base scene. For normal testing and building, use:

```text
Museum AR > Prepare Android Build And Run
```
